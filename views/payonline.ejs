<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <head>
  <meta charset="UTF-8">
  <title>Pay Online</title>
  <!-- pass your CSRF token into the page: -->
  <meta name="csrf-token" content="<%= csrfToken %>">
  <!-- your other CSS, meta, etc. -->
</head>

  <title>Pay Online</title>
  <!-- your global CSS, meta, etc. -->
  <style>
    /* hide card until you want to show it (optional) */
    .service-card.custom {
      display: block; /* or none if you still want a toggle here */
    }
  </style>
</head>
<body>
  <!-- header/nav… -->

  <main>
    <h1>Pay Online</h1>
    <div class="service-card custom" id="custom-card">
      <h3>Custom Amount</h3>
      <input
        type="number"
        id="custom-amount"
        placeholder="Enter amount"
        min="0.01"
        step="0.01"
      />
      <button class="buy-btn" data-service="custom">Pay Custom</button>
      <div class="paypal-button-container"></div>
    </div>
  </main>

  <!-- footer… -->

  <!-- PAYPAL INTEGRATION -->
   <script nonce="<%= nonce %>" src="/js/paypal.js"></script>

</body>
</html>
